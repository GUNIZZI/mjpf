import { Template } from '@/app/layout/default/ui/Template';

const Portfolio = () => {
    return (
        <Template>
            <div>
                <strong>Portfolio</strong>
            </div>
        </Template>
    );
};

export { Portfolio };
