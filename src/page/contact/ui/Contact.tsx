import { Template } from '@/app/layout/default/ui/Template';

const Contact = () => {
    return (
        <Template>
            <div>
                <strong>Contact</strong>
            </div>
        </Template>
    );
};

export { Contact };
